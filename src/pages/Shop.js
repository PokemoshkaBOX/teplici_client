import React from 'react';
import {Container} from "react-bootstrap";
import {observer} from "mobx-react";


const Shop = observer(() => {

    return (
        <Container >
            Привет
        </Container>
    );
});

export default Shop;